# forefst wininsiderrefs8gtest2.raw deleted --no-slack
# ----------------------------------------------------------------------------
==============================================================================
ReFS Deleted File Finder
==============================================================================
  Image:        wininsiderrefs8gtest2.raw
  ReFS version: 3.14
  Cluster size: 0x10000
  Checkpoints:  2 (VC: 161, 160)
  Objects:      132

── Trash Table (OID 0xD) ──
  Empty (all deletions fully processed)

── Checkpoint Comparison ──
  Current checkpoint: VC=161
  Previous checkpoint: VC=160
  Same top-level files in both (change may be in subdirectories or metadata)

── Recovery methods ──
  Mode: recovery.  Ran: Trash table (0xD), checkpoint diff.
  Slack scan SKIPPED (--no-slack). Run `deleted` (the default) to recover deleted rows.
  Fine-grained: --no-slack · --scan-pages · --orphans · --carve · --search SUB · --csv/--json/--jsonl FILE · `export deleted DIR`.
  --max-scan N stops the orphan scan early (faster, but the run is then INCOMPLETE); `help deleted` lists what each option costs.
  Also: `deleted --orphans` adds a low-confidence tier for OID-Table objects unlinked from the tree.
  Prior versions of files that still exist: `snapshots`.  Windows Recycle Bin: `recyclebin`.


