# forefst win11refstestmftecmd.raw specials
# ----------------------------------------------------------------------------
======================================================================
ReFS special-attribute inventory — win11refstestmftecmd.raw (ReFS 3.14)
======================================================================
  TYPE           COUNT   run `specials <type>` for the list
  ------------------------------------------------------------
  ads               84   named data streams
  reparse          444   symlinks / junctions / WSL links
  wsl                0   WSL / Linux-metadata links
  hardlink         450   multi-linked files (link groups)
  sparse             0   FILE_ATTRIBUTE_SPARSE_FILE (0x200)
  encrypted          0   EFS-encrypted ($EFS)
  compressed         0   WOF / compressed
  integrity        559   integrity checksum stream
  ea                 0   extended attributes / WSL EA
  snapshot           0   CoW prior-version streams
  ------------------------------------------------------------
  981 distinct objects carry >=1 special attribute.
  (per-type counts OVERLAP — one object can be several types at once, so the
   rows above sum to more than this de-duplicated total.)
  Deep ops: reparse --index · snapshots --extract · dataruns · export ads "file:stream"
